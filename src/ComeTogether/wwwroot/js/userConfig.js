angular.module('tasks_app').factory('userConfig', function ($http) {
    //var userName = {};
    
    ////if (userName != "")
    ////    return userName;

    //// GET CURRENT USER NAME
    //$http.get("/api/currentuser")
    //    .then(function (response) {
    //        //Success
    //        userName = response.data;
    //    }, function (error) {
    //    }).finally(function () { });

    return;
});